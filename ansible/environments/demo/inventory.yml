---
# Demo / production VM inventory example.
# Copy and adjust ansible_host, ansible_user, and SSH key path before provisioning.
all:
  children:
    pd_fade_servers:
      hosts:
        pd-fade-demo:
          ansible_host: 203.0.113.10
          ansible_user: root
          ansible_port: 22
          # ansible_ssh_private_key_file: ~/.ssh/id_ed25519
