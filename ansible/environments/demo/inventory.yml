---
# Demo VM. Override ansible_host / ansible_user / SSH key as needed.
all:
  children:
    pd_fade_servers:
      hosts:
        pd-fade-demo:
          ansible_host: 157.230.124.0
          ansible_user: root
          # ansible_ssh_private_key_file: ~/.ssh/id_ed25519
